# Java Date
> 当前时间

    Date now = new Date();
    SimpleDateFormat df = new SimpleDateformat("yyyy-MM-dd HH:mm:ss");
    String times = df.format(now);

> 昨天

    Date d = new Date();
    SimpleDateformat df = new SimpleDateformat("yyyy-MM-dd HH:mm:ss");
    long ys = d.getTime() - 24 * 60 * 60 * 1000;
    String times = df.format(ys);
